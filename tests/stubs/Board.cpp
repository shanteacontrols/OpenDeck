#include <inttypes.h>
#include "core/src/general/IO.h"

namespace Board
{
    namespace detail
    {
        namespace map
        {
            uint32_t adcChannel(const core::io::mcuPin_t& pin)
            {
                return 0;
            }
        }    // namespace map
    }        // namespace detail
}    // namespace Board