# APDS9960 Color Studio

Displays APDS9960 RGB readings as a large live color swatch. Recent detected colors are kept as chips along the bottom so it is easy to compare objects or lighting changes.

## Requirements

- Configure the board OSC destination to the computer running Processing.
- APDS9960 **RGB** enabled.
- Install the Processing oscP5 library.
