SOURCES      += $(CORE_MCU_SOURCES) $(CORE_USB_SOURCES)
INCLUDE_DIRS += $(CORE_MCU_INCLUDE_DIRS) $(CORE_USB_INCLUDE_DIRS) $(PROJECT_MCU_INCLUDE_DIRS)
LDFLAGS      += $(CORE_MCU_LDFLAGS)
LDLIBS       += $(CORE_MCU_LDLIBS)
CPPFLAGS     += $(CORE_MCU_CPPFLAGS)
DEFINES      += $(CORE_MCU_DEFINES) $(CORE_USB_DEFINES) $(PROJECT_MCU_DEFINES)
