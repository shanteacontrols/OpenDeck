config I2C_THREAD_SLEEP_MS
    int "I2C thread loop sleep in milliseconds"
    range 0 1000
    default 1
    help
      Sleep interval used by the synchronous I2C processing loop after
      each iteration. Set this to 0 in tests to yield instead of sleeping
      on wall-clock time.
